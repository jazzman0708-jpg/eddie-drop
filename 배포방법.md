# Eddie Drop 배포 방법

## 한 줄 요약

새 버전을 내놓을 때는 이것만 하면 됩니다.

```bash
./build/release.sh 1.2.0
```

`1.2.0` 자리에 새 버전 번호만 넣으세요. 나머지는 전부 자동입니다.

---

## 그 한 줄이 하는 일

| 순서 | 하는 일 |
|---|---|
| 1 | 버전 번호를 `CSXS/manifest.xml` 과 `plugin/plugin.json` 두 곳에 똑같이 써넣음 |
| 2 | 빠진 파일이 없는지 검사 |
| 3 | 기능 파일(`plugin/`)을 `dist/eddie-drop-plugin-1.2.0.zip` 으로 압축 |
| 4 | zip 의 **지문(sha256)** 계산 — 받은 파일이 바뀌지 않았는지 확인하는 용도 |
| 5 | `version.json` 을 새로 씀 — **사용자 패널은 이 파일 하나만 봅니다** |
| 6 | 깃허브에 커밋 · `v1.2.0` 태그 · 푸시 |
| 7 | 릴리스를 만들고 zip 을 첨부 |
| 8 | 주소가 실제로 살아 있는지 확인까지 |

끝나면 사용자가 패널에서 **설정 → 업데이트 → [업데이트 확인]** 을 눌렀을 때
새 버전이 바로 보입니다. **설치 파일을 다시 깔 필요가 없습니다.**

---

## 달라진 점 적기

적지 않으면 스크립트가 물어봅니다. 미리 적으려면:

```bash
./build/release.sh 1.2.0 --notes "효과음 검색이 빨라졌습니다"
```

여기 적은 글이 사용자 패널의 **설정 → 업데이트** 칸에 그대로 보입니다.

---

## 가끔 쓰는 옵션

```bash
# 올리기 전에 만들기만 해보기 (깃허브에 아무것도 안 올라감)
./build/release.sh 1.2.0 --dry-run

# 확인 묻지 않고 바로 올리기
./build/release.sh 1.2.0 --yes

# 이 버전보다 낮으면 못 쓰게 막기
./build/release.sh 1.2.0 --min 1.0.0

# 설치 파일을 새로 받아야 하는 큰 변경일 때
#   → 패널이 "설치 파일 받기" 버튼을 보여줍니다
./build/release.sh 2.0.0 --installer
```

---

## 버전 번호 정하는 법

`1.2.3` = `큰변경 . 기능추가 . 버그수정`

| 무엇을 했나 | 예시 |
|---|---|
| 오류만 고쳤다 | 1.2.0 → **1.2.1** |
| 기능을 추가했다 | 1.2.1 → **1.3.0** |
| 구조가 크게 바뀌었다 | 1.3.0 → **2.0.0** |

---

## 설치 파일(.pkg / .exe) 만들기

업데이트가 아니라 **처음 설치하는 사람용** 파일입니다.

### 맥 .pkg — 이 컴퓨터에서 바로

```bash
./build/build.sh mac
```
→ `dist/EddieDrop-<버전>-mac.pkg`

### 윈도우 .exe — 깃허브에서

윈도우 설치 파일은 **Inno Setup** 으로 만듭니다. 윈도우 전용이라 맥에서는 못 만듭니다.

`./build/release.sh` 로 태그를 올리면 **깃허브가 알아서 만들어 릴리스에 붙입니다.**

수동으로 만들려면:
```bash
gh workflow run build-windows.yml --repo jazzman0708-jpg/eddie-drop
```

만들 때마다 깃허브가 **실제로 설치해 보고** 확장 폴더 파일을 원본과 대조합니다.
없어야 할 파일이 하나라도 들어가면 빌드가 실패합니다
(그 파일 때문에 CEP 서명이 깨져 패널이 안 뜨기 때문).

자세한 건 [installer/README.md](installer/README.md) 를 보세요.
원본 스크립트는 **타이닛**님이 만들어 주셨습니다.

### 릴리스에 올리기

```bash
gh release upload v1.0.0 dist/EddieDrop-1.0.0-mac.pkg dist/EddieDrop-1.0.0-win.exe --clobber
```

### 서명 인증서에 대해

맥과 윈도우 설치 파일은 **같은 자체 서명 인증서**로 서명됩니다.
이 인증서는 두 곳에 있습니다.

| 어디 | 무엇 |
|---|---|
| `build/certs/selfsigned.p12` | 이 컴퓨터 (저장소에 안 올라감) |
| 깃허브 Secrets | `CEP_CERT_BASE64` · `CEP_CERT_PASSWORD` |

> ⚠️ **이 두 파일을 꼭 백업해 두세요.**
> 인증서가 바뀌면 사용자가 기존 확장을 지우고 다시 설치해야 할 수 있습니다.
> 잃어버렸다면 `./build/sign/make-cert.sh` 로 새로 만든 뒤
> 깃허브 Secrets 도 새 값으로 바꿔야 합니다.

## 무언가 잘못됐을 때

**올린 버전에 문제가 있다**
사용자 패널이 알아서 지켜줍니다 — 새 버전이 제대로 안 열리면
**다음에 켤 때 자동으로 이전 버전으로 되돌아갑니다.**
사용자가 직접 되돌리려면 설정 → 업데이트 → **[이전 버전으로 되돌리기]**.

급하면 고친 뒤 번호만 올려서 다시 배포하면 됩니다.

```bash
./build/release.sh 1.2.1 --notes "1.2.0 의 오류를 고쳤습니다"
```

**"gh 명령을 찾을 수 없습니다"**
새 터미널 창을 열고 다시 해보세요.

**"깃허브에 로그인돼 있지 않습니다"**
```bash
gh auth login
```

---

## 절대 하면 안 되는 것

- `.dev` 파일을 저장소에 올리지 마세요 — 사용자 쪽 업데이트가 매번 지워집니다
  (`.gitignore` 와 `build.sh` 가 막고 있습니다)
- **API 키를 코드에 적지 마세요** — 키는 사용자가 설정 탭에 직접 넣습니다
  (`~/Library/Application Support/Eddie/shared-settings.json`, 저장소 밖)
- 인증서(`.p12`)와 비밀번호는 저장소에 올리지 마세요 (`.gitignore` 로 막아둠)
