# Eddie Drop 설치 방법

프리미어 프로에서 스톡 영상 · 이미지 · GIF · 스티커 · 효과음을 찾아
바로 타임라인에 넣는 패널입니다.

**버전 1.0.1**

---

## 받는 곳

https://github.com/jazzman0708-jpg/eddie-drop/releases/latest

| 내 컴퓨터 | 받을 파일 |
|---|---|
| 맥 | **EddieDrop-1.0.1-mac.pkg** |
| 윈도우 | **EddieDrop-1.0.1-win.exe** |

---

## 설치하기 전에

**프리미어 프로를 완전히 종료해 주세요.** 켜둔 채로 설치하면 패널이 나타나지 않습니다.

---

## 맥에서 설치하기

### 1. 파일을 두 번 클릭합니다

### 2. ⚠️ 경고가 뜹니다 — 정상입니다

> **"확인되지 않은 개발자가 배포했기 때문에 열 수 없습니다"**

애플 개발자 인증서(연 129달러)를 쓰지 않아서 나오는 경고입니다.
파일에 문제가 있다는 뜻이 아닙니다.

**넘어가는 방법**

1. 받은 `.pkg` 파일을 **오른쪽 클릭** (또는 `Control` 키를 누른 채 클릭)
2. **열기** 를 선택
3. 다시 뜨는 창에서 **열기** 를 누르면 진행됩니다

> macOS 15 이상에서는 이 방법이 안 될 수도 있습니다. 그럴 때는
> **시스템 설정 → 개인정보 보호 및 보안** 으로 가면 화면 아래쪽에
> "EddieDrop… 이 차단되었습니다" 와 함께 **[그래도 열기]** 버튼이 있습니다.

### 3. 안내대로 계속 → 설치

- **관리자 암호를 묻지 않습니다.** 사용자 폴더 안에만 설치되기 때문입니다.
- 설치 위치: `~/Library/Application Support/Adobe/CEP/extensions/com.eddie.drop`

### 4. 프리미어를 켜고 **창 → 확장명 → Eddie Drop**

---

## 윈도우에서 설치하기

### 1. 파일을 두 번 클릭합니다

### 2. ⚠️ 파란 경고창이 뜹니다 — 정상입니다

> **"Windows의 PC 보호"**
> "Microsoft Defender SmartScreen에서 인식할 수 없는 앱의 시작을 차단했습니다"

마이크로소프트 코드 서명 인증서(연 수십만 원)를 쓰지 않아서 나오는 경고입니다.
새로 만든 프로그램은 다운로드 수가 쌓이기 전까지 대부분 이 경고가 뜹니다.

**넘어가는 방법**

1. **추가 정보** 를 클릭
2. 아래에 나타나는 **실행** 버튼을 클릭

### 3. 안내대로 다음 → 설치

- **관리자 권한이 필요 없습니다.**
- 설치 위치: `%APPDATA%\Adobe\CEP\extensions\com.eddie.drop`

### 4. 프리미어를 켜고 **창 → 확장명 → Eddie Drop**

---

## 설치 후 첫 세팅

패널을 처음 열면 **설정** 탭에서 API 키 4개를 넣어야 검색이 됩니다.
전부 **무료**이고, 설정 탭의 **[키 발급받기 ↗]** 링크에서 바로 받을 수 있습니다.

| 사이트 | 쓰이는 곳 |
|---|---|
| Pexels | 영상 · 이미지 |
| Pixabay | 영상 · 이미지 |
| GIPHY | GIF · 스티커 |
| Freesound | 효과음 |

키를 넣고 **[연결 테스트]** 를 눌러 `연결 성공` 이 뜨면 끝입니다.

> **내 컴퓨터에 있는 파일**은 키 없이도 **내 파일** 탭에서 바로 쓸 수 있습니다.

자세한 사용법은 [사용설명서](docs/사용설명서.md) 를 봐주세요.

---

## 기존 버전이 있을 때

그냥 새 설치 파일을 실행하면 됩니다.

- 기존 버전은 자동으로 정리됩니다
- **입력해 둔 API 키와 설정은 그대로 유지됩니다**
- 받아둔 소스 파일도 그대로 남습니다

---

## 업데이트

**설치 파일을 다시 받을 필요가 없습니다.**

패널 안에서 **설정 → 업데이트 → [업데이트 확인]** 을 누르면 됩니다.
프리미어를 켤 때 자동으로 확인하게 해둘 수도 있습니다 (기본 켜짐).

새 버전이 제대로 안 열리면 다음에 켤 때 **저절로 이전 버전으로 되돌아갑니다.**

---

## 지우고 싶을 때

### 맥
아래 폴더를 지우면 됩니다.
```
~/Library/Application Support/Adobe/CEP/extensions/com.eddie.drop
```

### 윈도우
**제어판 → 프로그램 제거 → Eddie Drop**

### 설정과 받아둔 파일까지 완전히 지우려면

지우개는 이 폴더들을 **일부러 남겨둡니다.** 직접 지우셔야 합니다.

```
맥     ~/Library/Application Support/Eddie      (API 키 · 설정)
       ~/Documents/Eddie Drop                   (받아둔 소스)

윈도우 %APPDATA%\Eddie
       내 문서\Eddie Drop
```

---

## 잘 안 될 때

| 증상 | 해결 |
|---|---|
| **메뉴에 Eddie Drop 이 없다** | 프리미어를 완전히 껐다 켜보세요. 설치할 때 프리미어가 켜져 있었다면 다시 설치해 주세요 |
| **패널이 열리는데 하얗다** | 잠깐 기다려 주세요. 처음 열 때 기능 파일을 준비합니다 |
| **"기능 파일을 준비하지 못했습니다"** | 아래 폴더를 지우고 패널을 다시 열어보세요<br>맥 `~/Library/Application Support/Eddie/plugins`<br>윈도우 `%APPDATA%\Eddie\plugins` |
| **맥에서 설치 파일이 안 열린다** | 위의 "오른쪽 클릭 → 열기" 를 해보세요 |
| **윈도우에서 SmartScreen 이 막는다** | "추가 정보 → 실행" 을 눌러주세요 |
| **검색이 안 된다** | 설정 탭에서 API 키를 넣고 [연결 테스트] 를 해보세요 |

---

## 지원 환경

| | |
|---|---|
| 프리미어 프로 | **24.0 이상** (2023년 10월~) |
| 맥 | macOS (인텔 · 애플 실리콘 모두) |
| 윈도우 | Windows 10 이상 |

---

## 문의하기

궁금한 점이나 잘 안 되는 부분이 있으면 메일로 보내주세요.

**rookietwo@naver.com**

패널의 **설정 → 문의하기 → [메일 보내기]** 를 누르면
버전 정보가 채워진 메일이 바로 열립니다.
